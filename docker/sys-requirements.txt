# This file contains system package dependencies. It is used for `apt-get` in
# Dockerfile and .gitlab-ci.yml. It is intended for the ubuntu:18.04 docker
# image.

build-essential
python3
python3-dev
python3-pip
python3-setuptools

# Python packages dependencies:
# for xmlsec.
libxmlsec1-dev
